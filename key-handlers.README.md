Key EventListeners are assigned in:
`webapp/alpha/vp.js`, search for "// Change spacebar behavior to Play/Pause"

Media Keys (next/prev/play/pause) are assigned in:
`webapp/assets/js/mstream.player.js`, search for "// Setup Media Session"